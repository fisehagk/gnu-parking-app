<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/secondary"/> <!-- Background color of the input field -->
    <corners android:radius="10dp"/> <!-- Radius of the rounded corners -->
    <stroke
        android:width="1dp"
    android:color="@color/secondary"/>
</shape>